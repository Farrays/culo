# ==============================================================================
# FARRAY'S INTERNATIONAL DANCE CENTER - ENVIRONMENT VARIABLES
# ==============================================================================
# Copy this file to .env and fill in your actual values
# Variables prefixed with VITE_ are exposed to the client-side code

# ------------------------------------------------------------------------------
# SENTRY ERROR TRACKING (Production)
# ------------------------------------------------------------------------------
# Get your DSN from: https://sentry.io/settings/projects/
# Used for: Error tracking, performance monitoring, user feedback
# Required: No (optional for development, recommended for production)
VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# App version for release tracking in Sentry
VITE_APP_VERSION=1.0.0

# Sentry Sample Rates (0.0 to 1.0)
# - tracesSampleRate: Percentage of transactions to track (performance)
# - replaysSessionSampleRate: Percentage of normal sessions to record
VITE_SENTRY_TRACES_SAMPLE_RATE=0.2
VITE_SENTRY_REPLAYS_SESSION_SAMPLE_RATE=0.1

# Environment (auto-detected, but can override)
# VITE_ENV=production

# ------------------------------------------------------------------------------
# GOOGLE ANALYTICS (Production)
# ------------------------------------------------------------------------------
# Get your Measurement ID from: https://analytics.google.com/
# Format: G-XXXXXXXXXX (GA4) or UA-XXXXXXXXX-X (Universal Analytics - deprecated)
# Used for: Website traffic analytics, user behavior tracking
# Required: No (optional, but recommended for SEO/marketing insights)
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ------------------------------------------------------------------------------
# BUILD CONFIGURATION
# ------------------------------------------------------------------------------
# Build mode: development | production | test
# Note: Vite automatically sets NODE_ENV based on command (dev/build)
NODE_ENV=production

# Public base URL for assets (used in production builds)
# Default: / (root)
# Example for subdirectory: /dance-center/
VITE_BASE_URL=/

# ------------------------------------------------------------------------------
# FEATURE FLAGS (Optional)
# ------------------------------------------------------------------------------
# Enable/disable features in production without code changes
# VITE_FEATURE_INSTAGRAM_FEED=true
# VITE_FEATURE_BOOKING_SYSTEM=false
# VITE_FEATURE_LIVE_CHAT=false

# ------------------------------------------------------------------------------
# THIRD-PARTY INTEGRATIONS (Optional)
# ------------------------------------------------------------------------------
# Instagram Access Token (for InstagramFeed component)
# VITE_INSTAGRAM_ACCESS_TOKEN=your-access-token

# Facebook Pixel ID (for marketing tracking)
# VITE_FACEBOOK_PIXEL_ID=123456789012345

# Google Maps API Key (if using embedded maps)
# VITE_GOOGLE_MAPS_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXX

# ------------------------------------------------------------------------------
# DEPLOYMENT SETTINGS (Vercel/Netlify)
# ------------------------------------------------------------------------------
# These are typically set automatically by the hosting platform
# VERCEL=1
# VERCEL_ENV=production
# VERCEL_URL=farrayscenter.com

# ------------------------------------------------------------------------------
# USAGE NOTES
# ------------------------------------------------------------------------------
# 1. Development: Copy to .env and use default values
# 2. Production: Set secrets in Vercel/Netlify dashboard (Environment Variables)
# 3. CI/CD: Set secrets in GitHub Actions (Settings → Secrets and variables)
# 4. Never commit .env to Git (already in .gitignore)
# 5. All VITE_ variables are embedded in the client bundle during build
# 6. Non-VITE_ variables are only available during build time (Node.js)

# ==============================================================================
# SECURITY REMINDER
# ==============================================================================
# ⚠️ VITE_ variables are PUBLIC - exposed in the browser bundle
# ⚠️ Never put sensitive data (API keys, passwords) in VITE_ variables
# ⚠️ For sensitive operations, use a backend API with proper authentication

