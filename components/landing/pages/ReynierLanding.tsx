import GenericDanceLanding from '../GenericDanceLanding';
import { REYNIER_LANDING_CONFIG } from '../../../constants/reynier-landing-config';

const ReynierLanding = (): React.ReactElement => (
  <GenericDanceLanding config={REYNIER_LANDING_CONFIG} />
);
export default ReynierLanding;
