import GenericDanceLanding from '../GenericDanceLanding';
import { TWERK_LANDING_CONFIG } from '../../../constants/twerk-landing-config';

const TwerkLanding = (): React.ReactElement => (
  <GenericDanceLanding config={TWERK_LANDING_CONFIG} />
);
export default TwerkLanding;
