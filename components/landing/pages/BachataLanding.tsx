import GenericDanceLanding from '../GenericDanceLanding';
import { BACHATA_LANDING_CONFIG } from '../../../constants/bachata-landing-config';

const BachataLanding = (): React.ReactElement => (
  <GenericDanceLanding config={BACHATA_LANDING_CONFIG} />
);
export default BachataLanding;
