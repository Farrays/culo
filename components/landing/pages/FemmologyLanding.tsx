import GenericDanceLanding from '../GenericDanceLanding';
import { FEMMOLOGY_LANDING_CONFIG } from '../../../constants/femmology-landing-config';

const FemmologyLanding = (): React.ReactElement => (
  <GenericDanceLanding config={FEMMOLOGY_LANDING_CONFIG} />
);
export default FemmologyLanding;
